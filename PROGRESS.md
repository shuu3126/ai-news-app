# PROGRESS — ai-news-app

## 現在の状態
**仕上げフェーズ完了・自動テスト全通過。ローカルコミット済み・未push（オーナー確認待ち）。**

- 実装: 完了（PWA一式・Gemini生成・Web Speech読み上げ・履歴
  ＋ PC向けレイアウト・音量調整・Googleドライブ同期・テーマ切替・読み上げ波形）
- テスト: `tests/test_all.mjs` **181項目 PASS / 0 FAIL**（モバイル420x900・PC1280x800の両方）
- 実API検証: 済み（コンテンツ方針転換後の原稿を実際に生成して確認。6,566字）
- git: **ローカルコミットまで。push はしていない**

---

## 次にやること（オーナー判断が必要）

1. **GoogleドライブのOAuthクライアントIDをどうするか（要判断）**
   - kakeibo-app と**同じクライアントIDを流用してよいか**、ai-news-app 用に新規発行するか
   - 流用できる条件: 承認済みJavaScript生成元に `https://shuu3126.github.io` が入っていること
     （kakeibo-app も GitHub Pages 配信なら、たいてい同じオリジンなのでそのまま動く）
   - 分けるメリット: 片方の連携を解除しても、もう片方に影響しない。ログに区別が付く
   - スコープは `drive.file` のみ。保存先は `ai-news-app-data / ai-news-data.json` で
     kakeibo-app（`kakeibo-app-data`）とは別フォルダなので、データが混ざる心配はない
2. **読み上げ波形の色（要判断）**
   - 現在は暖色 `--wave-color: #f2a33f`（ダークは `#ffb74d`）。
     テーマカラーの青一色に対するコントラスト目的でデザインAIが提案した色。
     ブランド上そろえたいなら `--accent-color` 系に変えるだけで済む（CSS変数1行）
3. **アイコンの再生成（要判断）**
   - 現行アイコンは同心円状のアークでWi-Fiアイコンに酷似しており、
     「音声」「AIニュース」の要素が薄い、というデザインAIの指摘がある
   - 作り直すならデザインAIへの再依頼が必要（今回は手を付けていない）
4. **専用のGemini APIキーにリファラー制限が付いているかの確認**
   - 公開URLで使うため、キーがブラウザに露出する
5. スマホ実機での動作確認（ホーム画面追加・日本語音声の有無・2台での同期の収束）

## 未対応・既知の制約

- **バックグラウンド再生ができない。** Web Speech API はタブが非アクティブになると
  ブラウザによって停止する。スマホで画面を消すと止まる。
  これを解消するにはサーバーサイドTTSで音声ファイルを生成し `<audio>` で再生する必要がある（別途要検討）
- **毎朝の自動生成はしていない。** ボタンを押したときだけ生成する。
  自動化するならサーバー or ローカルのタスクスケジューラが必要（PWA単体では不可能）
- **grounding が指定した情報源以外を拾うことがある。** 実測では Malay Mail / Forbes /
  SiliconANGLE など二次情報源からの引用が混ざった。プロンプトで情報源を限定しても
  Google検索の結果次第になる。精度を上げるならRSS直読みなど別方式が必要
- 生成には1〜3分かかる（2段階のAPI呼び出し＋grounding検索のため）

---

## セッションログ

### 2026-08-25 — 仕上げフェーズ（デザイン改善・PC対応・音量・ドライブ同期・コンテンツ方針転換）

**やったこと（5本立て）**

| # | 内容 | 主な変更ファイル |
|---|---|---|
| A | デザインAIのレビュー対応 | `css/style.css` `index.html` `js/app.js` |
| B | PC向けレイアウト（サイドナビ＋右レール） | `css/style.css` `index.html` |
| C | 音量調整 | `js/speech.js` `js/storage.js` `index.html` `js/app.js` |
| D | Googleドライブ同期 | `js/drivesync.js`(新) `js/sync.js`(新) `js/storage.js` |
| E | コンテンツ方針の転換 | `js/gemini.js` |

**E: コンテンツ方針の転換（オーナーからの直接フィードバック）**

「技術的な話はよく分からないし関心が持てない。実際にAIを使った事業の成功例、導入方法、
画期的なスクリプト、便利な使い方、Googleの最新技術と組み合わせた自動化例が読みたい」

`.company/tools/ai_news_digest.py`（Python版・既に方針転換済み）の文面を移植した。
**分量目標だけは 5000〜6000字のまま**（Python版は800〜1400字。用途が違うので意図的にズレている）。

- 情報源を差し替え: anthropic.com/news・simonwillison.net・Hacker News 中心
  → anthropic.com/customers・openai.com/stories・Google Workspace/Cloud AIブログ・
    TLDR AI/The Rundown AI/The Neuron・note.com/Zenn/Qiita・Product Hunt
- 「Claude/Anthropic最優先」という縛りを撤廃（技術ニュース偏重の原因だった）
- 冒頭『本日のAI関連ニュース』→『本日のAI活用ニュース』、
  締め『AIニュースダイジェスト』→『AI活用ダイジェスト』
- `_expandNarration()` / `_condenseNarration()` の中の文言も同時に揃えた
  （揃えないと、尺調整が走った回だけ言い回しが変わる）

実APIで検証した結果（`gemini-2.5-flash`・grounding あり）:

| 指標 | 結果 |
|---|---|
| 収集（1段階目） | 3,748字 |
| ナレーション（2段階目） | 6,566字（許容範囲内なので尺調整の再呼び出しは走らず） |
| 「副業」の出現 | 18回 |
| 「削減」「自動化」「売上」 | 9 / 6 / 2回 |
| 「ベンチマーク」「パラメータ」「資金調達」「論文」「規制」 | **すべて0回** |

内容も「工務店の問い合わせ対応を自動化して月40時間削減」「Gemini Notebookで提案資料作成を短縮」
など、狙いどおり事例・ノウハウ中心になった。

**D: ドライブ同期 — kakeibo-app で踏んだ2つの重大バグを最初から回避した**

1. `ensureDataFile()` は、ローカルに `driveFolderId` が無い場合でも
   **フォルダを作る前に必ず同名フォルダを検索する**（ファイル側も同様）。
   検索を省くと2台目の端末が別フォルダを作り、何度同期してもデータが交わらない。
   **PC1台だけのテストでは絶対に再現しない**種類のバグ
2. `sync()` のローカルスナップショット（`buildSyncPayload()`）は
   **必ず `await download()` の後**で取る。前に取ると、ダウンロード待ちの数秒間に
   保存されたダイジェストが後続の書き戻しで消える

どちらも自動テストで守るようにした（[16] のセクション）。特に2番目は、
`download()` の待ち時間中にわざと `saveDigest()` を走らせて、消えないことを検証している。

削除は論理削除（`deletedAt`）に変更した。物理削除のままだと、古いコピーを持つ端末が
同期した瞬間に消したはずの履歴が復活する。`loadAllDigests()`（墓標込み）を新設し、
`saveDigest()` / `deleteDigest()` / `clearDigests()` はすべてこちらを経由して書き戻す。
墓標は30日で捨てる。同じ日付の再生成は **id を引き継いで上書き**する
（id を振り直すと他端末では別レコードとして2件に増える）。

**レビューで直したバグ（Gemini生成物）**

| ファイル | 内容 |
|---|---|
| `js/app.js` | `drivesync.getStatus()` の戻り値は `signedIn` なのに `status.isSignedIn` を見ていた。**常に undefined になり、同期が一度も走らない**。3箇所を修正 |
| `css/style.css` | ダーク用の色上書きをファイル**冒頭**の `@media (prefers-color-scheme: dark)` に書いていた。`.status-message.status-loading` などの同一詳細度のライト用ルールが後ろに出てくるため、あとがち勝ちで打ち消され、**ダークモードで生成中バナーが白いまま浮いていた**（＝今回直すはずだったバグが直っていなかった）。末尾へ移動して解決。実機スクショで発見 |
| `css/style.css` | `.eq-bar` に `transform: scaleY()` だけ当てていて `height` が無く、`align-items: flex-end` の flex アイテムとして**高さ0になり波形が一切見えない**。`height: 100%` を追加 |
| `css/style.css` | `.recent-rail` の**デフォルトの `display: none` が抜けていた**。モバイルでも右レールが出てしまう |
| `css/style.css` | `body[data-theme]` の中にCSSのネスト記法を使っていた。フラットなセレクタに書き直し |

**テスト**

92項目 → **181項目**（89項目追加）。すべて PASS。

| セクション | 内容 |
|---|---|
| [12] 音量調整 | 保存・復元・`utterance.volume` への伝搬・範囲外のクランプ |
| [13] テーマの手動切り替え | `body[data-theme]` の付け外し、**ダークで生成中バナーが白く浮かないことを実測色で検証**（カスケード順の回帰テスト） |
| [14] 読み上げ波形 | 5本のバー・再生/一時停止/停止での `hidden` と `is-paused` |
| [15] 空状態・相対日付・声の表示名 | 「今日」「昨日」「3日前」・`primary-btn`/`ghost-btn`・`（日本語）`表記 |
| [16] 論理削除と同期マージ | 墓標・LWW・同着時の削除優先・`toTime()` のタイムゾーン混在・**download後スナップショット** |
| [17] PC幅レイアウト | サイドナビ化・`bottom: auto`・右レール・2カラム設定・シマー・波形の高さ |

PC幅の検証は**テストの最後**にまとめ、終わったら必ずモバイル解像度へ戻している。

**判断したこと**
- 右レール `#recent-rail` は `hidden` 属性を使わず CSS の `display` で出し分ける。
  ビュー切り替え（`hidden` 属性に統一というルール）に一切関与させないため
- PC幅のレイアウトは `#app` の `grid-template-areas` の組み替えだけで実現し、
  **DOMの並びは変えていない**（kakeibo-app と同じ方針）
- `handleDriveSignOut()` で `clearDriveRefs()` は呼ばない。
  フォルダID/ファイルIDを消すと、再ログイン時に別フォルダを作るリスクだけが増える
- 同期は `digests` の1コレクションのみ。設定（APIキー・声）は端末ごとに違ってよいので同期しない

**やっていないこと**
- push・デプロイ（**オーナーの明示的な指示待ち**）
- アイコンの再生成（デザインAIへの再依頼が必要）
- `.company/tools/ai_news_digest.py` の分量目標の変更（意図的に800〜1400字のまま）

### 2026-08-24 — 新規作成〜実装完了

**経緯**
当初 PowerShell + Windows内蔵TTS で試作し動作確認まで完了していたが、
オーナーから「わかりやすいUIが欲しい」「ブラウザで動かしたい」「スマホとPC両方で使いたい」
と追加要望があり、**`kakeibo-app` と同じ設計思想のブラウザPWAとして作り直す**方針に転換。

作業中にオーナーから分量目標の変更指示。
**800〜1,400字（3〜5分）→ 5,000〜6,000字（読み上げ約15分）** へ。

**やったこと**
1. `CLAUDE.md` / `PROGRESS.md` を先に作成（セキュリティ・技術制約を固定）
2. モジュール契約（export名・DOM ID・localStorageキー）を先に確定してから実装に入った
3. `gemini_bridge.py` へ3回に分けて委譲
   - コアロジック（`storage.js` / `gemini.js` / `speech.js`）
   - UI層（`index.html` / `css/style.css` / `js/app.js`）
   - テスト（`tests/test_all.mjs`）
4. 生成物をレビューして手で修正（下記「レビューで直したバグ」）
5. `sw.js` / `manifest.webmanifest` / `.gitignore` / GitHub Actions を作成
6. PWAアイコンを生成（Pillow。テーマ色の角丸背景＋白い音波）
7. Puppeteerで自動テスト → **92 PASS / 0 FAIL**
8. 実APIで2回検証し、プロンプトの分量チューニング

**分量チューニングの結果（実測）**

| 版 | 1段階目 | 2段階目 | 読み上げ目安 |
|---|---|---|---|
| 初版（「15件以上」「5000字以上」） | 8,119字 | **11,258字** | 約32分（長すぎ） |
| 改訂版（「10〜12件」「1トピック400〜500字」「6000字を超えない」） | 3,967字 | **5,373字** | **約15分（目標どおり）** |

Geminiは文字数の自己管理が苦手なので、プロンプトだけに頼らず
**トピック件数と1トピックあたりの文字数で間接的に制御**するのが効いた。
加えて生成後に実測して詰め直す `_condenseNarration()` / `_expandNarration()` を入れてある
（3,500字未満で増補・7,500字超で短縮。しきい値を緩めにしているのは、
毎回2回目のAPI呼び出しが走ると生成時間と課金が倍になるため）。

**レビューで直したバグ（Gemini生成物）**

| ファイル | 内容 |
|---|---|
| `speech.js` | `stop()` 直後に `speak()` すると、`cancel()` で積み残した古い utterance の `onend` が新セッションの `_stopped === false` を見て**新旧のチャンクが二重に進む**。セッション世代番号（`_session`）を導入して解決 |
| `speech.js` | チャンクごとに `await loadVoices()` していて繋ぎ目に無音の隙間ができる → 音声リストをキャッシュして同期取得に変更 |
| `speech.js` | 短い文を連結するときに**半角スペースを挟んでいた**（日本語の読み上げに不自然な間が入る）→ 挟まないように修正 |
| `storage.js` | `crypto.randomUUID` はセキュアコンテキスト限定。LANのhttp配信で落ちるのでフォールバック追加 |
| `app.js` | トーストを `transitionend` で隠していた。CSSにトランジションが無い環境で**消えないまま残る** → タイマーで確実に隠す |
| `app.js` | `speech.speak()` が `false`（未対応・空文字）を返したときUIが「再生中」のまま固まる → `resetSpeakUI()` を追加 |
| `app.js` | 別のダイジェストを開いても前の読み上げが止まらない → `showDigest()` で `speech.stop()` |
| `css/style.css` | トーストが `white-space: nowrap` で、長いエラーメッセージが画面外へはみ出して読めない → 折り返すよう修正 |

**テストで踏んだ落とし穴**（詳細は `tests/README.md`）
- `window.speechSynthesis` は getter のみのアクセサで、素の代入は**黙って失敗する**。
  端末の本物の音声が出てきて原因が分からなくなった。`Object.defineProperty` が必須
- APIキー検証のテストで `req.continue()` にしていたため、**本物のGemini APIへダミーキーで飛んでいた**

**判断したこと**
- サーバーサイドTTSは採用しない。Web Speech APIならサーバー費用ゼロ・PC/スマホ両対応で
  要件を満たせる。代償はバックグラウンド再生ができないこと（UIに注意書きを出して許容）
- Drive同期・ファイル同期は入れない。個人情報を扱わずデータも軽いので localStorage で十分
  （kakeibo-app からあえて削った部分）
- 履歴は7日分・物理削除。kakeibo-app のような論理削除（墓標）は同期しないので不要

**やっていないこと**
- push・GitHubリポジトリ作成・デプロイ（**オーナーの明示的な指示待ち**）
- `.company/tools/ai_news_digest.py`（Python版プロトタイプ）のプロンプト更新。
  こちらは 800〜1,400字のままなので、**JS版と分量目標がズレている**。
  Python版を今後も使うなら揃える必要がある（今回はスコープ外として手を付けていない）
